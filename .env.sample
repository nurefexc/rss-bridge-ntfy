# ntfy-bridge-rss environment variables

# Directory where feed configuration JSON files are located (default: configs)
CONFIG_DIR=configs

# ntfy server URL (default: https://ntfy.sh)
NTFY_URL=https://ntfy.sh

# ntfy authentication token (optional, if required by the server)
NTFY_TOKEN=your_ntfy_token_here

# Path to the SQLite database file for history
DB_PATH=rss_history.db

# System timezone (default: UTC)
TZ=UTC

# Synchronization interval in seconds (default: 600, i.e., 10 minutes)
SYNC_INTERVAL=600

# Custom User-Agent for HTTP requests
USER_AGENT=Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36
